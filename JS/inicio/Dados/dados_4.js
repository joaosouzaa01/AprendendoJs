let a = 4

// Você consegue manipular a variavel (let) de qualquer forma

//mudando o valor no decorrer do codigo, ex.
a = 8

// ou adicionando mais atributos ex.

// a = a + 12

// Quando o valor for alterado no decorrer do codigo, o que vai valer é o ultimo Atruibuido, no caso a linha 10

// alterando o LET para CONST vc colocara um valor que não poderá ser modificado no decorrer do codigo

const b = 3

// O LET e o VAR podem ser alterados, o const não, por ser justamente uma constante